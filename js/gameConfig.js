class GameConfig{

    // Canvas window config
    canvasWidth = 670;
    canvasHeight = 400;

    // Blocks config
    blockRowCount = 5;
    blockColumnCount = 10;
    blockHeight = 20;
    blockWidth = 60;
    blockPadding = 5;
    blockTopOffset = 10;
    blockLeftOffset = 10;
    blockColor = '#064789'

    // Ball config
    ballRadius = 7;
    ballX = 300;
    ballY = 300;
    ballVerticalSpeed = -2;
    ballHorizontalSpeed = 2;
    ballPositionAngle = 0;
    ballColor = '#EBF2FA';

    // Paddle config
    paddleX = 300;
    paddleY = 380;
    paddleWidth = 60;
    paddleHeight = 10;
    paddleColor = '#427AA1';
    paddleSpeed = 5;

}